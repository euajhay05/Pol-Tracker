# ShootTracker

Production and finance tracker for Pol Film Productions — shoots (kanban + calendar), clients, income, expenses, loans, savings goals, and document generation (contracts/quotations/invoices as PDF).

Standalone HTML/CSS/JS, no build step required. Open `index.html` directly, or serve the folder with any static file server. Data persists to the browser's `localStorage`.
